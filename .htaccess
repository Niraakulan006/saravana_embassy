#ERROR 404 PAGE
ErrorDocument 404 /404.html

# RewriteEngine On
# RewriteCond %{SERVER_PORT} 80
# RewriteCond %{HTTP_HOST} ^(www\.)?\.com [NC]
# RewriteRule ^(.*)$ https://www..com/$1 [R=301,L]

### Redirect non-www => www ###
# RewriteCond %{HTTP_HOST} !^www..com$ [NC]
# RewriteRule ^(.*)$ https://www..com/$1 [L,R=301]

### Redirect index to www..com ###
# RewriteCond %{THE_REQUEST} ^.*/index\.php
# RewriteRule ^(.*)index.php$ /$1 [R=301,L]

# Extra Security Headers
<IfModule mod_headers.c>
Header set X-XSS-Protection "1; mode=block"
Header always append X-Frame-Options SAMEORIGIN
Header set X-Content-Type-Options nosnif
</IfModule>

RewriteEngine On

# If the request is for an existing directory or file, do nothing
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME} !-f

# Remove .php from URL
RewriteRule ^([^/]+)/?$ $1.php [L]
 RewriteRule ^category/([^/]+)$ category_view.php?url=$1 [L,QSA]
 RewriteRule ^category/?$ category.php [L,QSA]

